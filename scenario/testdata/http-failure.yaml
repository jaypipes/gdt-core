name: HTTP failure
description: test expected failure of HTTP API call
require:
 - books_api
 - books_data
defaults:
  http:
    base_url: http://127.0.0.1:4000
tests:
 - name: no such book was found
   GET: /books/nosuchbook
   response:
     json:
       length: 0
     status: 404
